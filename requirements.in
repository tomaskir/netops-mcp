# Core dependencies
# Cap below MCP SDK 2.0, which removed mcp.server.fastmcp; the HTTP transport
# needs the fastmcp 2.x API (FastMCP.http_app(path=...)).
mcp>=1.9.0,<2.0.0
fastmcp>=2.0.0,<3.0.0
pydantic>=2.0.0
psutil>=5.9.0

# HTTP and networking
requests>=2.31.0
httpx>=0.25.0
starlette>=0.35.0

# Security
python-dotenv>=1.0.0

# Optional: FastMCP for HTTP transport (the `server` extra folded into core in 2.x)
fastmcp>=2.0.0,<3.0.0
